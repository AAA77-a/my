import React, { useState } from 'import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
constimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', 'import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 1580import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 165import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85,import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91,import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pdimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot asimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    'import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    'import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95,import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 320import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', dataimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabelimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as npimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=10import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
printimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 2import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.perimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default functionimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | nullimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string |import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    ifimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSectionimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <divimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-whiteimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-autoimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between itemsimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</aimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-50import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <sectionimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 pyimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-2import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mbimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relaxed">
            数据分析是通过收集、清洗、转换和解释数据，从中发现import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relaxed">
            数据分析是通过收集、清洗、转换和解释数据，从中发现有价值的信息和洞察的过程。
            本课程将系统学习数据分析的基本import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relaxed">
            数据分析是通过收集、清洗、转换和解释数据，从中发现有价值的信息和洞察的过程。
            本课程将系统学习数据分析的基本概念、常用工具和实用技术。
          </p>
        </div>
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relaxed">
            数据分析是通过收集、清洗、转换和解释数据，从中发现有价值的信息和洞察的过程。
            本课程将系统学习数据分析的基本概念、常用工具和实用技术。
          </p>
        </div>
      </section>

      {/* 知识点内容 */}
      <section className="py-16import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据示例
const dataExamples = {
  sales: `# 销售数据分析示例
import pandas as pd
import matplotlib.pyplot as plt

# 创建示例数据
data = {
    '月份': ['1月', '2月', '3月', '4月', '5月', '6月'],
    '销售额': [12500, 15800, 13200, 18900, 16500, 21000],
    '订单数': [85, 102, 91, 120, 108, 135]
}

df = pd.DataFrame(data)
print("销售数据：")
print(df)
print("\\n统计摘要：")
print(df.describe())`,
  visualization: `# 数据可视化示例
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 创建数据
data = {
    '产品': ['A', 'B', 'C', 'D', 'E'],
    '销量': [120, 85, 150, 95, 110],
    '利润': [4500, 3200, 5200, 3800, 4200]
}

df = pd.DataFrame(data)

# 创建可视化
plt.figure(figsize=(10, 6))
sns.barplot(x='产品', y='销量', data=df, color='skyblue')
plt.title('产品销量对比')
plt.xlabel('产品')
plt.ylabel('销量')
plt.show()`,
  statistics: `# 统计分析示例
import numpy as np
import pandas as pd

# 创建样本数据
np.random.seed(42)
data = np.random.normal(loc=100, scale=15, size=100)

# 统计分析
print("样本数据：", data[:10])
print("\\n描述性统计：")
print(f"均值: {np.mean(data):.2f}")
print(f"中位数: {np.median(data):.2f}")
print(f"标准差: {np.std(data):.2f}")
print(f"最小值: {np.min(data):.2f}")
print(f"最大值: {np.max(data):.2f}")
print(f"25%分位数: {np.percentile(data, 25):.2f}")
print(f"75%分位数: {np.percentile(data, 75):.2f}")`
};

export default function DataAnalysis() {
  const [activeSection, setActiveSection] = useState<string | null>(null);
  const [activeSubSection, setActiveSubSection] = useState<string | null>(null);

  const toggleSection = (section: string) => {
    if (activeSection === section) {
      setActiveSection(null);
      setActiveSubSection(null);
    } else {
      setActiveSection(section);
      setActiveSubSection(null);
    }
  };

  const toggleSubSection = (subsection: string) => {
    if (activeSubSection === subsection) {
      setActiveSubSection(null);
    } else {
      setActiveSubSection(subsection);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 font-sans">
      {/* 导航栏 */}
      <nav className="bg-white text-emerald-700 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex justify-between items-center">
          <div className="text-xl font-light tracking-wide">黄安德</div>
          <div className="flex space-x-6">
            <Link to="/" className="hover:text-emerald-500 transition-colors text-sm font-medium">首页</Link>
            <a href="/#courses" className="hover:text-emerald-500 transition-colors text-sm font-medium">课程</a>
            <a href="/#about" className="hover:text-emerald-500 transition-colors text-sm font-medium">关于我</a>
          </div>
        </div>
      </nav>

      {/* 页面标题 */}
      <section className="bg-gradient-to-r from-emerald-50 to-teal-50 py-16">
        <div className="container mx-auto px-4 text-center">
          <div className="inline-block mb-6">
            <div className="w-24 h-1 bg-emerald-400 mx-auto mb-6"></div>
            <h1 className="text-4xl font-light text-gray-800 mb-4">数据分析技术</h1>
            <div className="w-24 h-1 bg-emerald-400 mx-auto"></div>
          </div>
          <p className="max-w-3xl mx-auto text-gray-600 leading-relaxed">
            数据分析是通过收集、清洗、转换和解释数据，从中发现有价值的信息和洞察的过程。
            本课程将系统学习数据分析的基本概念、常用工具和实用技术。
          </p>
        </div>
      </section>

      {/* 知识点内容 */}
      <section className="py-16 bg-white">
        <div className="container mx-auto px-4">
          {/*import React, { useState } from 'react';import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePointsimport React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   -import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3.import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度规约
   - 数值规约
    `
  },
  {
    id:import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度规约
   - 数值规约
    `
  },
  {
    id: 2,
    title: '描述性统计',
    description: '集中趋势、import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度规约
   - 数值规约
    `
  },
  {
    id: 2,
    title: '描述性统计',
    description: '集中趋势、离散程度、分布形态的基本统计量计算和解释。',
    content: `
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度规约
   - 数值规约
    `
  },
  {
    id: 2,
    title: '描述性统计',
    description: '集中趋势、离散程度、分布形态的基本统计量计算和解释。',
    content: `
## 描述性统计指标

### 集中趋势
- 均值 (Mean)
-import React, { useState } from 'react';
import { Link } from 'react-router-dom';

// 数据分析知识点
const knowledgePoints = [
  {
    id: 1,
    title: '数据预处理',
    description: '数据清洗、缺失值处理、异常值检测、数据转换和标准化的基本技术。',
    content: `
## 数据预处理的重要性

数据预处理是数据分析的第一步，通常占据整个分析过程的70-80%时间。

## 主要步骤

1. **数据清洗**
   - 处理缺失值
   - 去除重复数据
   - 处理异常值

2. **数据转换**
   - 标准化
   - 归一化
   - 对数转换

3. **数据集成**
   - 合并多个数据源
   - 处理数据冲突

4. **数据规约**
   - 维度规约
   - 数值规约
    `
  },
  {
    id: 2,
    title: '描述性统计',
    description: '集中趋势、离散程度、分布形态的基本统计量计算和解释。',
    content: `
## 描述性统计指标

### 集中趋势
- 均值 (Mean)
- 中位数 (Median)
- 众数 (Mode)

### 离散程度
